# Capstone-
Capstone project for Professional Certificate in Machine Learning &amp; AI course 
Promped used to generate the code: 
Function 1

 

I am doing a python coding assignment, whereby I need to maximise a function. Every submission I provide 2 inputs and get back an output from that function.

The past inputs are stored in a CSV file C:\Users\ofire\OneDrive\Documents\Study\AI\Imperial\Capstone\Function1.csv. the first 2 columns are prior inputs submitted inputs and the third Colum is prior outputs received from the function. 

Please design a python code that would maximise the function using a variety of machine learning techniques using the relationships between inputs and outputs.  The output of the code should be the next parameters as 2 numbers between 0 and 1 with 6 digits after the zero each and a dash between them.  The code should also list the methods it tried to use to maximise the function, the results for each method as well as what was the method the code selected as well as why this method was selected. The code should also print out that these are the results for function 1. Please provide only the code without explanation.

 

Function 2

I am doing a python coding assignment, whereby I need to maximise a function. Every submission I provide 2 inputs and get back an output from that function.

The past inputs are stored in a CSV file C:\Users\ofire\OneDrive\Documents\Study\AI\Imperial\Capstone\Function2.csv. the first 2 columns are prior inputs submitted inputs and the third Colum is prior outputs received from the function. 

Please design a python code that would maximise the function using a variety of machine learning techniques using the relationships between inputs and outputs.  The output of the code should be the next parameters as 2 numbers between 0 and 1 with 6 digits after the zero each and a dash between them.  The code should also list the methods it tried to use to maximise the function, the results for each method as well as what was the method the code selected as well as why this method was selected. The code should also print out that these are the results for function 2. Please provide only the code without explanation.

 

 

Function 3

I am doing a python coding assignment, whereby I need to maximise a function. Every submission I provide 3 inputs and get back an output from that function.

The past inputs are stored in a CSV file C:\Users\ofire\OneDrive\Documents\Study\AI\Imperial\Capstone\Function3.csv. the first 3 columns are prior inputs submitted inputs and the fourth Colum is prior outputs received from the function. 

Please design a python code that would maximise the function using a variety of machine learning gaussian techniques using the relationships between inputs and outputs.  The output of the code should be the next parameters as 3 numbers between 0 and 1 with 6 digits after the zero each and a dash between them. The sum total sum of the 2 numbers selected should be equal to 1. The numbers selected should not be o and should not be 1. 
The code should also list the methods it tried to use to maximise the function, the results for each method as well as what was the method the code selected as well as why this method was selected. The code should also print out that these are the results for function 3. Please provide only the code without explanation.

 
Function 4

I am doing a python coding assignment, whereby I need to maximise a function. Every submission I provide 4 inputs and get back an output from that function.

The past inputs are stored in a CSV file C:\Users\ofire\OneDrive\Documents\Study\AI\Imperial\Capstone\Function4.csv. the first 4 columns are prior inputs submitted inputs and the fifth Colum is prior outputs received from the function. 

Please design a python code that would maximise the function using a variety of gaussian machine learning techniques using the relationships between inputs and outputs.  The output of the code should be the next input parameters as 4 numbers between 0 and 1 with 6 digits after the zero each and a dash between them. The code should also list the methods it tried to use to maximise the function, the results for each method as well as what was the method the code selected as well as why this method was selected. The code should also print out that these are the results for function 4. Please provide only the code without explanation.


 

Function 5

I am doing a python coding assignment, whereby I need to maximise a function. Every submission I provide 4 inputs and get back an output from that function.

The past inputs are stored in a CSV file C:\Users\ofire\OneDrive\Documents\Study\AI\Imperial\Capstone\Function5.csv. the first 4 columns are prior inputs submitted inputs and the fifth Colum is prior outputs received from the function. 

Please design a python code that would maximise the function using a variety of machine learning gaussian techniques using the relationships between inputs and outputs.  The output of the code should be the next parameters as 4 numbers between 0 and 1 with 6 digits after the zero each and a dash between them. The sum total sum of the 2 numbers selected should be equal to 1. The numbers selected should not be 0 and should not be 1. 
The code should also list the methods it tried to use to maximise the function, the results for each method as well as what was the method the code selected as well as why this method was selected. The code should also print out that these are the results for function 5. Please provide only the code without explanation.

 

 

Function 6

I am doing a python coding assignment, whereby I need to maximise a function. Every submission I provide 5 inputs and get back an output from that function.

The past inputs are stored in a CSV file C:\Users\ofire\OneDrive\Documents\Study\AI\Imperial\Capstone\Function6.csv. the first 5 columns are prior inputs submitted inputs and the sixth Colum is prior outputs received from the function. 

Please design a python code that would maximise the function using a variety of machine learning gaussian techniques using the relationships between inputs and outputs.  The output of the code should be the next parameters as 5 numbers between 0 and 1 with 6 digits after the zero each and a dash between them. The sum total sum of the 2 numbers selected should be equal to 1. The numbers selected should not be 0 and should not be 1. 
The code should also list the methods it tried to use to maximise the function, the results for each method as well as what was the method the code selected as well as why this method was selected. The code should also print out that these are the results for function 6. Please provide only the code without explanation.

 Function 7

I am doing a python coding assignment, whereby I need to maximise a function. Every submission I provide 6 inputs and get back an output from that function.

The past inputs are stored in a CSV file C:\Users\ofire\OneDrive\Documents\Study\AI\Imperial\Capstone\Function7.csv. the first 6 columns are prior inputs submitted inputs and the seventh Colum is prior outputs received from the function. 

Please design a python code that would maximise the function using a variety of machine learning gaussian techniques using the relationships between inputs and outputs.  The output of the code should be the next parameters as 6 numbers between 0 and 1 with 6 digits after the zero each and a dash between them. The sum total sum of the 2 numbers selected should be equal to 1. The numbers selected should not be 0 and should not be 1. 
The code should also list the methods it tried to use to maximise the function, the results for each method as well as what was the method the code selected as well as why this method was selected. The code should also print out that these are the results for function 7. Please provide only the code without explanation.
 

Function 8

I am doing a python coding assignment, whereby I need to maximise a function. Every submission I provide 8 inputs and get back an output from that function.

The past inputs are stored in a CSV file C:\Users\ofire\OneDrive\Documents\Study\AI\Imperial\Capstone\Function8.csv. the first 8 columns are prior inputs submitted inputs and the ninth Colum is prior outputs received from the function. 

Please design a python code that would maximise the function using a variety of machine learning techniques using the relationships between inputs and outputs.  The output of the code should be the next parameters as 8 numbers between 0 and 1 with 6 digits after the zero each and a dash between them.  The code should also list the methods it tried to use to maximise the function, the results for each method as well as what was the method the code selected as well as why this method was selected. The code should also print out that these are the results for function 8. Please provide only the code without explanation.
